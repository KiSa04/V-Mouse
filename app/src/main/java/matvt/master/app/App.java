/*
 * This Java source file was generated by the Gradle 'init' task.
 */
package matvt.master.app;

import com.kisa.vmouse.list.LinkedList;

import static com.kisa.vmouse.utilities.StringUtils.join;
import static com.kisa.vmouse.utilities.StringUtils.split;
import static matvt.master.app.MessageUtils.getMessage;

import org.apache.commons.text.WordUtils;

public class App {
    public static void main(String[] args) {
        LinkedList tokens;
        tokens = split(getMessage());
        String result = join(tokens);
        System.out.println(WordUtils.capitalize(result));
    }
}
